FROM python:3.12-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml uv.lock ./
RUN uv sync

# src/ is bind-mounted at runtime for hot reload
EXPOSE 8000

CMD ["uv", "run", "uvicorn", "style_workbench.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
